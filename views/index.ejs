<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script>
      const status = "<%- JSON.stringify(status) %>";

      if (JSON.parse(status) == "verified") {
        if (confirm("Deseja logar?")) {
          window.location.href = "http://localhost:3988/bazar-gratis/login";
        }
      }
    </script>
    <style>
      input[type="password"] {
        /* width: 70%; */
        box-sizing: border-box;
        text-align: center;
        margin: auto;
        display: block;
        padding: 0.6vw;
        min-width: 20vw;
        min-height: 2vw;
        margin-bottom: 1vh;
        border: none;
        border-radius: 0.25rem;
        font-size: max(1vw, 10px);
        color: #ffffff;
        background-color: #245285;
      }

      input[type="submit"] {
        background-color: #c9407d;
        border: none;
        color: white;
        text-decoration: none;
        text-align: center;
        cursor: pointer;
        margin: auto;
        display: block;
        color: white;
        min-width: 20vw;
        padding: 0.6vw;
        border-radius: 0.25rem;
        margin-bottom: 2vh;
        font-size: max(1.2vw, 12px);
      }
      h1 {
        text-align: center;
        color: #245285;
      }
    </style>
  </head>
  <body>
    <h1>Alterar Senha</h1>
    <form>
      <input
        type="password"
        name="password"
        id="password"
        placeholder="Senha"
      /><br />
      <input
        type="password"
        name="confirmPassword"
        id="confirmpassword"
        placeholder="Confirmar senha"
      /><br />
      <input type="submit" value="Alterar senha" />
    </form>
  </body>
</html>
